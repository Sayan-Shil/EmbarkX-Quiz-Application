spring:

  mail:
    host: smtp.gmail.com
    port: 587
    username: sayansil2000@gmail.com  # Change this
    password: fybx valw zguq fqbk      # Use App Password, not regular password
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000

  flyway:
    enabled: true
    baseline-on-migrate: true
    clean-disabled: false
    repair-on-migrate: true
    locations: classpath:db/migration  # ADD THIS LINE

  datasource:
    url: jdbc:mysql://localhost:3306/embarkx_quiz_app?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC
    username: root
    password: Sayan@CSE2004@Sayan
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 5
      connection-timeout: 30000

  jpa:
    hibernate:
      ddl-auto: none  # ADD THIS - Let Flyway handle schema, not Hibernate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect  # ADD THIS

server:
  port: 8081
  error:
    include-message: always
    include-binding-errors: always
    include-stacktrace: on_param
    include-exception: false

logging:
  level:
    root: INFO
    com.embarkx.embarkxquiz: DEBUG